# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Built executables
/shurli
shurli-*
cmd/keytool/keytool
relay-server/relay-server

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env
.DS_Store

# Testing and archived files
archived/

# Coverage data (unit + Docker integration merge)
coverage/

# Configuration files (keep only samples in repo)
*.yaml
!*.sample.yaml
authorized_keys
relay_authorized_keys
!configs/*.sample.yaml
!test/**/compose.yaml
!website/hugo.yaml
!.github/workflows/*.yaml

# Claude Code project instructions (private, never commit)
CLAUDE.md

# Local checks file (user-created, one command per line)
.checks

# Key files (private keys should never be committed)
*.key

# Relay server backup snapshots
**/backups/
